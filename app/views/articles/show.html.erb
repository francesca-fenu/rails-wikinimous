<h2><%= @article.title %></h2>
<p>Content: <%= @article.content %></p>

<%= link_to "Update article", edit_article_path %><br>
<%= link_to "Delete article",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure you want to delete #{@article.title}?" } %><br>
<%= link_to "Go back to all articles", articles_path %>