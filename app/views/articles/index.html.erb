<h2>All Articles</h2>
<ul>
  <% @articles.each do |article| %>
    <li>Title: <%= link_to article.title, article_path(article) %> - Content: <%= article.content %></li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>