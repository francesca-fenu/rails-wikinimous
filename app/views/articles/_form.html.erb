<div class="container">
  <%= simple_form_for(article) do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <% if article.new_record? %>
      <%= f.submit "Create Article" %>
    <% else %>
      <%= f.submit "Update Article" %>
    <% end %>
  <% end %>
</div>
